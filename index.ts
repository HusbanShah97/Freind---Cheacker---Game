
let isFreind: string = 'Husban';
if (isFreind === 'Husban' || isFreind === 'Sarim') {
    console.log(`${isFreind} is your freind`);
    
    
} else {
    console.log(`${isFreind} is not your freind`);
    
}